import 'package:flutter/material.dart';

class EncryptionPage extends StatelessWidget {
  const EncryptionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Ecryption Page.")));
  }
}
